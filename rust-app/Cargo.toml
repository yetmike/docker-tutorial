[package]
name = "rust-app"
version = "0.1.0"
edition = "2021"

[dependencies]
# axum — ergonomic async web framework built on hyper/tokio
axum = "0.7"
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
